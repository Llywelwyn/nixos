# focus-or-launch <window-class> <command...>
# activate the first window whose class matches, otherwise launch the command
class="$1"
shift
id=$(kdotool search --class "$class" 2>/dev/null | head -n1)
if [ -n "$id" ]; then
  exec kdotool windowactivate "$id"
fi
exec "$@"
